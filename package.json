{
  "name": "qingfu-envoy",
  "private": true,
  "version": "0.0.1",
  "description": "青蚨使 · Qingfu Envoy — open agent payment control plane (propose → human approve → licensed rail)",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test -w @qingfu/core",
    "build": "npm run build -w @qingfu/core"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
