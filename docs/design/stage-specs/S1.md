# Stage S1 Spec（契约对齐）

> 依赖：**S0 DoD 全绿**；来源：implementation-plan-v1 § S1

## DoD（机器可验证断言——stage-gate 逐条执行）

- [ ] `npm test` 退出码 0
- [ ] `npm run build` 退出码 0
- [ ] 未 `approved` 调用 execute → 失败（契约测试）
- [ ] `frozen` Envoy 不可 propose
- [ ] `frozen` **不**阻止已 `approved` 的 execute（ADR 004）
- [ ] `executed` 可 requestRefund → `refunded` 或 `refund_failed`
- [ ] 导出 API 无 `silent`/`autoPay`/`executeWithoutApproval` 符号
- [ ] `prd-domain-traceability.md` P0-1…P0-9 仍无空洞

## TDD 网格

| 功能 | 规范 | 失败测试 | 实现 |
|------|------|----------|------|
| 禁未批执行 | P0-3 | `execution.refuse-unapproved.test.ts` | execution |
| 急停语义 | 004 | `envoy.freeze-semantics.test.ts` | envoy+execution |
| 退款状态机 | P0-9 | `refund.state.test.ts` | RefundService+MockRail |
| 无静默符号 | P0-7 | `no-silent-symbols.test.ts` | 删静默 API |

## 产出物

- [ ] `packages/core` 与 `domain-model.md` 对齐

## 边界

- 不做 CLI/MCP/Web  
- 不做支付宝真调用
