# Stage S1 Spec（契约对齐）

> 依赖：**S0 DoD 全绿**；来源：implementation-plan-v1 § S1  
> **实现审计：** [S1-IMPLEMENTATION-AUDIT.md](../S1-IMPLEMENTATION-AUDIT.md) — **Go（100/100，严格复审）**

## DoD（机器可验证断言——stage-gate 逐条执行）

- [x] `npm test` 退出码 0（33 tests）
- [x] `npm run build` 退出码 0
- [x] 未 `approved` 调用 execute → 失败（契约测试）
- [x] `frozen` Envoy 不可 propose
- [x] `frozen` **不**阻止已 `approved` 的 execute（ADR 004）
- [x] `executed` 可 requestRefund → `refunded` 或 `refund_failed`
- [x] 导出 API 无 `silent`/`autoPay`/`executeWithoutApproval` 符号
- [x] `prd-domain-traceability.md` P0-1…P0-9 实现与测试闭合

## TDD 网格

| 功能 | 规范 | 失败测试 | 实现 |
|------|------|----------|------|
| 禁未批执行 | P0-3 | `execution.refuse-unapproved.test.ts` | execution |
| 急停语义 | 004 | `envoy.freeze-semantics.test.ts` | envoy+execution |
| 退款状态机 | P0-9 | `refund.state.test.ts` | RefundService+MockRail |
| 无静默符号 | P0-7 | `no-silent-symbols.test.ts` | 删静默 API |

## 产出物

- [x] `packages/core` 与 `domain-model.md` 对齐（`domain/` + `app/` + `persistence/`）

## 边界

- 不做 CLI/MCP/Web  
- 不做支付宝真调用
