# Stage S2 Spec（CLI 垂直切片）

> 依赖 S1 DoD 全绿

## DoD（机器可验证断言）

- [x] S1 DoD 仍全绿
- [x] CLI：propose → approve → execute(Mock) → export JSON → requestRefund
- [x] CLI freeze 后拒绝新 propose
- [x] CLI 冒烟测试或脚本退出码 0
- [x] `packages/cli/README.md` 含 ≤10 分钟 dogfood 步骤

## 产出物

- [x] `packages/cli`

## 边界

- 不做 MCP/Web / 支付宝
